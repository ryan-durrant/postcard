angular.module("app").controller("mainCtrl", function($scope, $timeout){
  // $scope.message = function(){
    $scope.messageText = "Click on the countries that you have visited to add some color to your world!";
  // };
  //
  // $timeout($scope.message, 1500);
});
